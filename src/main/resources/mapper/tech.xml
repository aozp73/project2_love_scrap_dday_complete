<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.jobara.tech.model.TechRepository">
      <select id="findAll" resultType="shop.mtcoding.jobara.tech.model.Tech">
        select * from tech_tb
      </select>

      <select id="findById" resultType="shop.mtcoding.jobara.tech.model.Tech">
        select * from tech_tb where id = #{id}
      </select>    
      
      <insert id="insert">
            insert into tech_tb(board_id, user_id, java, c_lang, python, php, jsc, ruby, assembly_lang, sql_lang, created_at)
            values(#{boardId}, #{userId}, #{java}, #{cLang}, #{python}, #{php}, #{jsc}, #{ruby}, #{assemblyLang}, #{sqlLang}, now())
      </insert>

      <update id="updateById">
        update tech_tb 
           set board_id = #{boardId}, 
               user_id = #{userId}, 
               java = #{java},
               c_lang = #{cLang},
               python = #{python},
               php = #{php},
               jsc = #{jsc},
               ruby = #{ruby},
               assembly_lang = #{assemblyLang},
               sql_lang = #{sqlLang}
            where id = #{id} 
    </update>      

    <delete id="deleteById">
        delete from user_tb where id = #{id} 
    </delete>
</mapper>